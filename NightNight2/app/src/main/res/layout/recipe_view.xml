<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/recipeContainer"
        android:layout_width="0dp"
        android:layout_height="450sp"
        android:background="@color/grey_primary"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/viewRecipeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:fontFamily="@font/comfortaa_semibold"
        android:text="@string/brf_add"
        android:textColor="@color/black"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/recipeContainer" />

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/recipeDescription"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/rectangle_grey_border"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        app:layout_constraintBottom_toBottomOf="@+id/recipeDescriptionContainer"
        app:layout_constraintEnd_toEndOf="@+id/recipeDescriptionContainer"
        app:layout_constraintStart_toStartOf="@+id/recipeDescriptionContainer"
        app:layout_constraintTop_toTopOf="@+id/recipeDescriptionContainer" />

    <ScrollView
        android:id="@+id/recipeDescriptionContainer"
        android:layout_width="0dp"
        android:layout_height="300sp"
        android:layout_marginTop="30dp"
        app:layout_constraintEnd_toEndOf="@+id/viewRecipeName"
        app:layout_constraintStart_toStartOf="@+id/viewRecipeName"
        app:layout_constraintTop_toBottomOf="@+id/viewRecipeName">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>